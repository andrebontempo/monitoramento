# =============================================================
# Alterações necessárias no prometheus.yml
# em /opt/docker/monitoramento/prometheus.yml
# =============================================================

# Adicionar logo após "global:" (antes de "scrape_configs:"):

rule_files:
  - /etc/prometheus/rules/*.yml

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - alertmanager:9093

# =============================================================
# Também atualizar o volume do Prometheus no docker-compose.yml
# para montar a pasta de regras:
#
#   prometheus:
#     volumes:
#       - ./prometheus.yml:/etc/prometheus/prometheus.yml
#       - ./prometheus-rules:/etc/prometheus/rules          ← ADICIONAR ESTA LINHA
#       - prometheus_data:/prometheus
# =============================================================
